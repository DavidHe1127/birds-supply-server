# all parrots
query {
  parrots(first:2) {
    edges {
      node {
        id
        code
        name
        lifespan
      }
    }
  }
}

# a single parrot
query {
  parrot(id: "5a1a05b48ca26513ae6c7785") {
    name
  }
}
