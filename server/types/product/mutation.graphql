input addProductInput {
  price: Int
  qty: Int
  parrotCode: String
}

input delProductInput {
  id: ID!
}

type Mutation {
  addProduct(input: addProductInput!): Product
  delProduct(input: delProductInput!): Product
}
