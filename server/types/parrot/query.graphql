type Parrot implements Node {
  id: ID!
  name: String
  size: Int
  mass: Float
  distribution: String
  lifespan: Int
  species: String
  code: String
}

type ParrotConnection {
  pageInfo: PageInfo!
  # a list of edges
  edges: [ParrotEdge]
  count: Int!
}

type ParrotEdge {
  cursor: String
  node: Parrot
}

type Query {
  parrot(id: ID, code: String): Parrot
  parrots(
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): ParrotConnection
}
