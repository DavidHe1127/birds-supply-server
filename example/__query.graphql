# basic query
query getCustomerByEmail {
  customers(email: "a@abc.com.au") {
    id
    first_name
  }
}

# with argument
query getCustomerById($id: ID!) {
  customers(id: $id) {
    id
    first_name
  }
}
